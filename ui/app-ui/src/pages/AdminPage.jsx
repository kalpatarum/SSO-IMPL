import React from 'react';
import { Container, Typography } from '@mui/material';

export default function AdminPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Admin Page</Typography>
      <Typography>Placeholder for Admin management.</Typography>
    </Container>
  );
}
